{% extends 'layouts/includes.html.twig' %}
{% set pageTitle = 'ačiū' %}

{% block body %}
    <div class="col-lg-offset-2 col-lg-8">
        <div class="text-center">
            <img width="300px" class="img-circle img-auto" src="{{ asset('bundles/site/img/good_dogo.jpeg') }}">
        </div>
        <div class="text-center">
            <h1>Ačių kad užpildėte klausimyną!</h1>
            <div style="font-size: 3em">
                <a href="{{ path('home') }}">Pagrindinis</a>
            </div>
        </div>
    </div>
{% endblock %}
